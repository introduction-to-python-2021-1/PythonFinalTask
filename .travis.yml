language: python
python:
  - "3.9"
install:
  - pip install -r requirements.txt
script:
  - pip install .
  - python setup.py nosetests --cover-branches --with-coverage --cover-min-percentage=50 --cover-package=. --nologcapture
  - pycodestyle --max-line-length=120 .
  - cd -