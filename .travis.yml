language: python
python:
  - "3.9"
install:
  - pip install -r requirements.txt
script:
  - cd rss_reader
  - pip install .
  - nosetests --cover-branches --with-coverage --cover-package=rss_reader .
  - pycodestyle --max-line-length=120 .
  - cd -